def find_Urls (url):
    # list of urls
    urls = []
    # Starting point 
    href_index = url.find("href='", 0)
    
    # while the hrer != -1 
    #   continue the search
    while href_index != -1:
        '''
        
        
        '''
        # storing the urls, without the href= part
            # Into the urls list
        urls.append()
